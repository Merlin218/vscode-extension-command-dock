{
  "name": "command-dock",
  "displayName": "Command Dock",
  "description": "A powerful command panel with Command Dock for executing terminal commands in your workspace",
  "version": "1.0.0",
  "publisher": "CommandDock",
  "icon": "public/icon.png",
  "engines": {
    "vscode": "^1.74.0"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "command",
    "terminal",
    "buttons",
    "dock",
    "custom",
    "executor",
    "workspace",
    "automation"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/Merlin218/vscode-extension-command-dock.git"
  },
  "bugs": {
    "url": "https://github.com/Merlin218/vscode-extension-command-dock/issues"
  },
  "homepage": "https://github.com/Merlin218/vscode-extension-command-dock#readme",
  "license": "MIT",
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "commandDock.addButton",
        "title": "Add Custom Button",
        "category": "Command Dock",
        "icon": "$(add)"
      },
      {
        "command": "commandDock.removeButton",
        "title": "Remove Custom Button",
        "category": "Command Dock",
        "icon": "$(trash)"
      },
      {
        "command": "commandDock.editButton",
        "title": "Edit Custom Button",
        "category": "Command Dock",
        "icon": "$(edit)"
      },
      {
        "command": "commandDock.refreshPanel",
        "title": "Refresh",
        "icon": "$(refresh)"
      },
      {
        "command": "commandDock.executeFromPanel",
        "title": "Execute Command",
        "icon": "$(play)"
      },
      {
        "command": "commandDock.addButtonFromPanel",
        "title": "Add Button",
        "icon": "$(add)"
      },
      {
        "command": "commandDock.editButtonFromPanel",
        "title": "Edit Button",
        "icon": "$(edit)"
      },
      {
        "command": "commandDock.removeButtonFromPanel",
        "title": "Remove Button",
        "icon": "$(trash)"
      },
      {
        "command": "commandDock.openSettings",
        "title": "Open Command Dock Settings",
        "category": "Command Dock",
        "icon": "$(gear)"
      }
    ],
    "views": {
      "explorer": [
        {
          "id": "commandDockPanel",
          "name": "Command Dock",
          "when": "workspaceFolderCount > 0 && config.commandDock.panelLocation == 'explorer'"
        }
      ],
      "scm": [
        {
          "id": "commandDockPanel",
          "name": "Command Dock",
          "when": "workspaceFolderCount > 0 && config.commandDock.panelLocation == 'scm'"
        }
      ]
    },
    "menus": {
      "view/title": [
        {
          "command": "commandDock.addButtonFromPanel",
          "when": "view == commandDockPanel",
          "group": "navigation@1"
        },
        {
          "command": "commandDock.refreshPanel",
          "when": "view == commandDockPanel",
          "group": "navigation@2"
        },
        {
          "command": "commandDock.openSettings",
          "when": "view == commandDockPanel",
          "group": "navigation@3"
        }
      ],
      "view/item/context": [
        {
          "command": "commandDock.executeFromPanel",
          "when": "view == commandDockPanel && viewItem == commandDockButton",
          "group": "inline@1"
        },
        {
          "command": "commandDock.editButtonFromPanel",
          "when": "view == commandDockPanel && viewItem == commandDockButton",
          "group": "inline@2"
        },
        {
          "command": "commandDock.removeButtonFromPanel",
          "when": "view == commandDockPanel && viewItem == commandDockButton",
          "group": "inline@3"
        },
        {
          "command": "commandDock.executeFromPanel",
          "when": "view == commandDockPanel && viewItem == commandDockButton",
          "group": "context@1"
        },
        {
          "command": "commandDock.editButtonFromPanel",
          "when": "view == commandDockPanel && viewItem == commandDockButton",
          "group": "context@2"
        },
        {
          "command": "commandDock.removeButtonFromPanel",
          "when": "view == commandDockPanel && viewItem == commandDockButton",
          "group": "context@3"
        }
      ]
    },
    "configuration": {
      "title": "Command Dock",
      "properties": {
        "commandDock.buttons": {
          "type": "array",
          "default": [],
          "description": "Command Dock configuration",
          "items": {
            "type": "object",
            "properties": {
              "id": {
                "type": "string",
                "description": "Unique identifier for the button"
              },
              "name": {
                "type": "string",
                "description": "Display name of the button"
              },
              "command": {
                "type": "string",
                "description": "Terminal command to execute"
              },
              "icon": {
                "type": "string",
                "description": "Icon for the button (optional)"
              },
              "color": {
                "type": "string",
                "description": "Button color (optional)"
              }
            },
            "required": [
              "id",
              "name",
              "command"
            ]
          }
        },
        "commandDock.autoCloseTerminal": {
          "type": "boolean",
          "default": false,
          "description": "Automatically close terminal after command execution"
        },
        "commandDock.panelLocation": {
          "type": "string",
          "default": "explorer",
          "enum": [
            "explorer",
            "scm"
          ],
          "enumDescriptions": [
            "Show in Explorer panel (left sidebar)",
            "Show in Source Control panel (left sidebar)"
          ],
          "description": "Choose where to display the Command Dock panel"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "pnpm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "pnpm run compile && pnpm run lint",
    "lint": "eslint src --ext ts",
    "lint:fix": "eslint src --ext ts --fix",
    "test": "node ./out/test/runTest.js",
    "clean": "rimraf out *.vsix",
    "package": "vsce package",
    "publish": "vsce publish",
    "publish:pre": "vsce publish --pre-release",
    "version:patch": "pnpm version patch --no-git-tag-version",
    "version:minor": "pnpm version minor --no-git-tag-version",
    "version:major": "pnpm version major --no-git-tag-version",
    "prepublishOnly": "pnpm run lint && pnpm run test",
    "release": "bash scripts/release.sh",
    "release:patch": "bash scripts/release.sh patch",
    "release:minor": "bash scripts/release.sh minor",
    "release:major": "bash scripts/release.sh major",
    "release:pre": "bash scripts/release.sh patch --pre-release",
    "install:local": "pnpm run compile && pnpm run package && code --install-extension *.vsix",
    "uninstall:local": "code --uninstall-extension CommandDock.command-dock"
  },
  "devDependencies": {
    "@types/vscode": "^1.74.0",
    "@types/node": "16.x",
    "@typescript-eslint/eslint-plugin": "^5.45.0",
    "@typescript-eslint/parser": "^5.45.0",
    "eslint": "^8.28.0",
    "typescript": "^4.9.4",
    "@vscode/vsce": "^2.22.0",
    "rimraf": "^5.0.5"
  }
}
